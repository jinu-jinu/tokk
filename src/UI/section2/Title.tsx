import BubbleText from "../../components/BubbleText";

const Title = () => {
  const title = ["W", "H", "A", "T", "'", "S", " ", "T", "O", "K", "K", "?"];
  return <BubbleText title={title} />;
};

export default Title;
